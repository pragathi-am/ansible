- name: install pacakges
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: install pkgs in loop
    ansible.builtin.package:
      name: "{{ item }}"
      state: present
    loop:
    - mysql
    - nginx

    